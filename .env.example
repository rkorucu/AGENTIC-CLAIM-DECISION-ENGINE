# Agent-service: optional, for LLM explanation + reflection
OPENAI_API_KEY=optional

# Backend: URL of agent-service (used when running via docker-compose)
AGENT_SERVICE_URL=http://agent-service:8000

# Frontend: API base URL (browser calls this; use localhost when running locally)
VITE_API_BASE_URL=http://localhost:8080/api
